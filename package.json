{
  "name": "yt-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "diag": "node -v && yt-dlp --version && ffmpeg -version | head -n1",
    "start": "npm run diag && node index.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-cache": "^5.1.2",
    "p-limit": "^6.2.0"
  }
}
